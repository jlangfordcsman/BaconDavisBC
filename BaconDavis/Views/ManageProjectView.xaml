<UserControl x:Class="BaconDavis.Views.ManageProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <ListBox ItemsSource="{Binding ActiveProjects}" SelectionMode="Single" 
                 SelectedItem="{Binding SelectedProject}" DisplayMemberPath="Name"/>
        
        <DockPanel Grid.Column="1">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <Label Content="New Project Name:" FontWeight="Bold" />
                <TextBox Text="{Binding NewProjectName}" Width="150"/>
                <Button Command="{Binding AddCommand}" Content="Add" />
            </StackPanel>
            
            <Button Content="Done" Command="{Binding DoneCommand}" DockPanel.Dock="Bottom" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        </DockPanel>
    </Grid>
</UserControl>
